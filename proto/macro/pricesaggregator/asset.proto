syntax = "proto3";
package macro.pricesaggregator;

import "gogoproto/gogo.proto";

option go_package = "github.com/vuong177/macro/x/prices-aggregator/types";

message Asset {
    // id of asset on chain
    uint64 id = 1;
    // denom of token on macro chain
    string denom = 2;
    // symbol of token
    string symbol = 3;
}